#include "include/sequence.h"
#include "include/submax.h"


int main (int argc, char * argv[])
{
    int test[] = {1, -8, 4, -5, -5, 3, 2, -1, 3, 0};
    m3 (test, 10);
    //Seq res = m2 (test, 9);
    //res.print(&res);
    return 0;
}